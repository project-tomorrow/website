before_script:
        - npm install express
        - npm install ejs
        - npm install colors
        - whoami
test:
        script:
                - killall -qw nodejs
                - echo 'La commande qui suit est l mal'
                - cp config/config.js.example config/config.js
                - nodejs server.js &
