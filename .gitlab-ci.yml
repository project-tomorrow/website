before_script:
        - npm install express
        - npm install ejs
        - npm install colors 
test:
        script:
                - echo 'La commande qui suit est l mal'
                - cp config/config.js.example config/config.js
                - nodejs -v
                - nodejs server.js
